# CLAUDE.md

All project instructions live in **[AGENTS.md](AGENTS.md)** — a single source of truth
shared by every agent tool (architecture, commands, house style, `.agents/rules/`,
issue tracking, session-completion protocol).

This file is intentionally just a pointer. Do not duplicate guidance here; edit
`AGENTS.md` instead.

<!--
  beads note: the BEADS INTEGRATION block is owned by AGENTS.md, not this file.
  Do NOT run `bd setup claude` — it would re-inject the block into this pointer.
  bd's git hooks (`bd prime` on SessionStart/PreCompact) live in .claude/settings.json
  and are unaffected by keeping this file a pointer.
-->
